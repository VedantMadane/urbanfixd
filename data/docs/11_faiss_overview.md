# FAISS Overview

FAISS is a library for efficient similarity search over dense vectors.
For small and medium datasets, `IndexFlatIP` provides exact nearest-neighbor retrieval.

At larger scales, FAISS supports approximate indexes such as IVF and HNSW for lower latency.
