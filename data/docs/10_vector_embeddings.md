# Embeddings and Semantic Search

An embedding maps text to dense vectors so semantic similarity can be measured numerically.
Cosine similarity is commonly used to compare query and document vectors.

`all-MiniLM-L6-v2` is a lightweight model that balances quality and local inference speed.
