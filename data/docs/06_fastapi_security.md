# FastAPI Security Basics

Security dependencies can parse and verify bearer tokens before route logic runs.
Common patterns include OAuth2 password flow and JWT bearer authentication.

Authentication proves identity; authorization checks if a user can access a resource.
