# Python Dataclasses

Dataclasses reduce boilerplate for data containers by auto-generating `__init__`, `__repr__`,
and optional comparison methods.

They are useful for representing ingestion outputs such as document chunks and metadata payloads.
