# Python Typing

Type hints improve readability and tooling support. Common hints include `list[str]`, `dict[str, int]`,
and `Optional[T]`.

FastAPI uses typing metadata to build validation rules and OpenAPI schemas.
