# Python asyncio Fundamentals

`asyncio` is Python's standard library framework for asynchronous I/O.
Coroutines are declared with `async def` and paused with `await`.

The event loop schedules tasks cooperatively, which is effective for many concurrent network requests.
